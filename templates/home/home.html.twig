{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
{# /* <style>
    .example-wrapper {
        margin: 1em auto;
        max-width: 800px;
        width: 95%;
        font: 18px/1.5 sans-serif;
    }

    .example-wrapper code {
        background: #F5F5F5;
        padding: 2px 6px;
    }
</style> */ #}

{# <div class="example-wrapper">
    <h3>Hello {{ controller_name }}! ✅</h3>
</div> #}
{# creer un tableau users #}
{# {% set users = ["alin","mans","playboy"] %} #}
{# notre boucle for dont user prend les valeur de users #}
{# <p>{% for user in users %}</p>
{{user}}
{% endfor %} #}

{# pour les date #}
{# {{"now"|date("d/m/y","Europe/Paris")}}

{% set name = "moi" %}
{{name}}

{% set number = 1988 %}
{{number}} #}
{# on a inclu une ni #}
{# {% include 'home/myinclude.html.twig' %} #}
{# on veut afficher notre tableau associatif ici en forme de card #}
{# <div class="d-flex flex-wrap w-5">

    {% for articles in article1 %}
    <div class="card" style="width: 18rem;">

        <img src="{{articles.image}}" class="card-img-top" alt="...">
        <div class="card-body">
            <p class="card-text">{{articles.title}}</p>
            {# <p class="card-text">{{articles.content}}</p> #}
            {#
        </div>
    </div>
    {% endfor %}
</div>
#}

{# formateur #}

<div class="row">
    {% for article in articles %}
    <div class="card col-md-4" style="width: 18rem;">
        <p class="date">{{ article.createdAt | date("d/m/y")}}</p>
        <img src="{{article.image}}" class="card-img-top" alt="{{ article.title }}">
        <div class="card-body">
            <h5 class="card-title"> {{article.title }}</h5>
            {# avec la method path 'show'le nom de la methode, {'id'est la clé cest comme ça: article.id ce qui sera envoyer dans methode})}}" #}
            <a href="{{path('show', {'id': article.id})}}" class="btn btn-primary">En Savoir plus</a>
        </div>
    </div>
    {% endfor %}
</div>

{% endblock %}